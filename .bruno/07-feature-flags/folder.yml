info:
  name: 07 Feature flags
  type: folder
  seq: 7
request:
  auth: inherit
docs: |-
  Per-BAN boolean feature flags. One endpoint, one request per flag the
  integration reads. Mirrors EngieBeClient::async_get_feature_flag.
